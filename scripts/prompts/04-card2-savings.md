# Card 2: Savings

Inside the wrapper div (the 4-column grid), add the second card with the same style as the first (dashed border, rounded, white background).

Inside this card, create these components:
- A title text component "📈 Savings" at the top
- A line chart component showing savings trend over 6 months (use teal color)
- A big bold text component "$5,839" showing the total amount
- A small red text component "🔻 -11% last week" showing the trend

Use proper typography and chart components, not raw HTML.
